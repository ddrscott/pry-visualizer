require "spec_helper"

RSpec.describe Pry::Visualizer do
  it "has a version number" do
    expect(Pry::Visualizer::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
